const Sensors = () => {
  return (
    <div className="container bg-base-100 px-4">
      <h1 className="text-2xl fg-primary">Sensors</h1>
      <p>Add sensors and configure settings and device associations</p>
    </div>
  );
};

export default Sensors;
